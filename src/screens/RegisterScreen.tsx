// Email/password registration was removed — VibeChat uses Google Sign-In only.
// This stub remains because the workspace doesn't allow file deletion via
// this tool. Safe to delete manually with `rm src/screens/RegisterScreen.tsx`.
export {};
